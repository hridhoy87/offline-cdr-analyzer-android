<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.OfflineCDRAnalyzer" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your dark theme here. -->
        <item name="dashboardBackgroundColor">@color/dashboard_bg_dark</item>
        <item name="primaryTextColor">@color/text_primary_dark</item>
        <item name="secondaryTextColor">@color/text_secondary_dark</item>
        <item name="cardBackgroundColorCustom">@color/card_bg_dark</item>
        <item name="hintTextColorCustom">@color/hint_dark</item>
        <item name="inputBackgroundColor">@color/input_bg_dark</item>
    </style>
</resources>